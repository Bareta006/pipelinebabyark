<!-- /sections/article-cards.liquid -->

<section class="section section-article-cards" 
  id="section-{{ section.id }}"
  data-section-id="{{ section.id }}"
  data-section-type="article-cards">
  
  <div class="section__header">
    {% if section.settings.title != blank %}
      <h2 class="section__title">
        {{ section.settings.title }}
      </h2>
    {% endif %}
    
    {% if section.settings.text != blank %}
      <div class="section__text">
        {{ section.settings.text }}
      </div>
    {% endif %}
  </div>
  
  <flickity-carousel>
    <div
      data-carousel
      data-options='{ "pageDots": false, "prevNextButtons": true, "wrapAround": true, "contain": true, "cellAlign": "left", "draggable": true, "groupCells": true, "isWrapping": true }'
      class="article-cards-carousel">
      {% for block in section.blocks %}
        <div class="article-card-slide" {{ block.shopify_attributes }} data-carousel-slide>
          {% render 'article-card', 
            image: block.settings.image,
            logo: block.settings.logo,
            text: block.settings.text,
            button_text: block.settings.button_text,
            button_url: block.settings.button_url,
            button_style: block.settings.button_style,
            button_color: block.settings.button_color
          %}
        </div>
      {% endfor %}
    </div>
  </flickity-carousel>
</section>

<style>
  #section-{{ section.id }} {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    margin-bottom: 3.75rem;
    width: 100%;
    max-width: 100%;
    background-color: {{ section.settings.section_bg_color }};
  }

  #section-{{ section.id }} .section__header {
    margin-bottom: 2.5rem;
    padding-left: 1.25rem;
    padding-right: 1.25rem;
    text-align: {{ section.settings.title_alignment }};
  }
  
  #section-{{ section.id }} .section__title {
    margin-bottom: 1rem;
    font-weight: 500;
    color: {{ section.settings.title_color }};
    font-size: {{ section.settings.title_font_size }}px;
  }
  
  #section-{{ section.id }} .section__text {
    margin-bottom: 2rem;
    color: {{ section.settings.text_color }};
    font-size: {{ section.settings.text_font_size }}px;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }
  
  /* Article card carousel styles */
  #section-{{ section.id }} .article-cards-carousel {
    width: 100%;
    padding-right: 2.1375rem;
    padding-left: 2.1375rem;
  }
  
  #section-{{ section.id }} .article-card-slide {
    width: 25%; /* 4 cards per view on desktop */
    min-width: 280px;
    padding: 0 1rem 0 1rem;
  }
  
  /* Article Card Styles */
  #section-{{ section.id }} .article-card {
    background-color: {{ section.settings.card_bg_color }};
    border-radius: 15px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    height: 100%;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        min-height: 427px;
  }
  
  #section-{{ section.id }} .article-card__image {
    width: 100%;
    position: relative;
  }
  
  #section-{{ section.id }} .article-card__main-image {
    width: 100%;
    display: block;
    object-fit: cover;
  }
  
  #section-{{ section.id }} .article-card__logo {
    padding: 1.25rem 1.25rem 0;
    max-width: 120px;
    position: relative;
  }
  
  #section-{{ section.id }} .article-card__logo-image {
    display: block;
    max-width: 100%;
  }
  
  #section-{{ section.id }} .article-card__text {
    padding: 1.25rem;
    font-size: {{ section.settings.card_text_font_size }}px;
    color: {{ section.settings.card_text_color }};
    flex-grow: 1;
  }
  
  #section-{{ section.id }} .article-card__button {
    padding: 0 1.25rem 1.25rem;
    margin-top: auto;
  }
  
  /* Carousel navigation */
  #section-{{ section.id }} .flickity-button {
    position: absolute;
    bottom: -4.125rem;
    top: auto;
    color: #220E53;
    border: 1px solid;
    z-index: 10;
    background-color: rgba(255, 255, 255, 0.8);
    border-radius: 50%;
    width: 44px;
    height: 44px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    opacity: 1;
    display: flex ;
    align-items: center;
    justify-content: center;
    visibility: visible ;
  }
  
  #section-{{ section.id }} .flickity-button:hover {
    background-color: #fff;
  }
  #section-{{ section.id }} .flickity-button:disabled {
    opacity: 0.5;
  }
  #section-{{ section.id }} .flickity-button-icon {
    fill: #333;
  }
  
  #section-{{ section.id }} .flickity-prev-next-button.previous {
    right: 5rem;
    left: auto;
  }
  
  #section-{{ section.id }} .flickity-prev-next-button.next {
    right: 1.25rem;
  }
  
  /* Override flickity's hiding of buttons */
  #section-{{ section.id }} .flickity-button.flickity-prev-next-button {
    display: none;
  }
  #section-{{ section.id }} .article-card__button a{
letter-spacing: 0;
    text-transform: none;
    font-weight: 400;
    padding: 7px 15px;
    font-size: 14px;
  } 

  /* Responsive styles */
  @media screen and (max-width: 749px) {
    #section-{{ section.id }} .section__title {
      font-size: {{ section.settings.title_font_size_mobile }}px;
    }
    
    #section-{{ section.id }} .section__text { 
      font-size: {{ section.settings.text_font_size_mobile }}px;
    }
    
    #section-{{ section.id }} .article-card__text {
      font-size: {{ section.settings.card_text_font_size_mobile }}px;
    }
    
    #section-{{ section.id }} .article-card {
      border-radius: 20px;
      min-height: 400px;
    }
    
    #section-{{ section.id }} .article-card-slide {
      width: 100%; /* 1 card per view on mobile */
      padding: 0.625rem;
    }
    
    #section-{{ section.id }} .flickity-button {
      width: 36px;
      height: 36px;
      
    } 
    
    #section-{{ section.id }} .flickity-prev-next-button.previous {
      right: 4rem;
    }
      /* Override flickity's hiding of buttons */
  #section-{{ section.id }} .flickity-button.flickity-prev-next-button {
    display: flex;
  }
  #section-{{ section.id }} .article-cards-carousel {
    padding-left: 0;
  }
  #section-{{ section.id }} {
    background-color: #fff;
  }
  }
</style>

{% schema %}
{
  "name": "Article Cards",
  "settings": [
    {
      "type": "header",
      "content": "Section Heading"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Featured Articles"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "title_font_size",
      "min": 20,
      "max": 72,
      "step": 1,
      "unit": "px",
      "label": "Title font size (desktop)",
      "default": 40
    },
    {
      "type": "range",
      "id": "title_font_size_mobile",
      "min": 16,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Title font size (mobile)",
      "default": 28
    },
    {
      "type": "select",
      "id": "title_alignment",
      "label": "Title alignment",
      "default": "center",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ]
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Browse our collection of featured articles and resources.</p>"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#333333"
    },
    {
      "type": "range",
      "id": "text_font_size",
      "min": 12,
      "max": 28,
      "step": 1,
      "unit": "px",
      "label": "Text font size (desktop)",
      "default": 16
    },
    {
      "type": "range",
      "id": "text_font_size_mobile",
      "min": 10,
      "max": 22,
      "step": 1,
      "unit": "px",
      "label": "Text font size (mobile)",
      "default": 14
    },
    {
      "type": "header",
      "content": "Card Design"
    },
    {
      "type": "color",
      "id": "card_bg_color",
      "label": "Card background color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "card_text_color",
      "label": "Card text color",
      "default": "#333333"
    },
    {
      "type": "range",
      "id": "card_text_font_size",
      "min": 12,
      "max": 22,
      "step": 1,
      "unit": "px",
      "label": "Card text font size (desktop)",
      "default": 16
    },
    {
      "type": "range",
      "id": "card_text_font_size_mobile",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Card text font size (mobile)",
      "default": 14
    },
    {
      "type": "header",
      "content": "Section Design"
    },
    {
      "type": "color",
      "id": "section_bg_color",
      "label": "Section background color",
      "default": "#F5F5F5"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "default": 50
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "default": 50
    }
  ],
  "blocks": [
    {
      "type": "article_card",
      "name": "Article Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Main image"
        },
        {
          "type": "image_picker",
          "id": "logo",
          "label": "Logo"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Card text",
          "default": "Share information about this article or resource."
        },
        {
          "type": "header",
          "content": "Button"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button text",
          "default": "Read More"
        },
        {
          "type": "url",
          "id": "button_url",
          "label": "Button URL"
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button style",
          "default": "btn",
          "options": [
            { "value": "btn", "label": "Solid button"},
            { "value": "btn--soft", "label": "Soft button"},
            { "value": "btn--outline", "label": "Outlined button"}
          ]
        },
        {
          "type": "select",
          "id": "button_color",
          "label": "Color",
          "default": "btn--primary",
          "options": [
            { "value": "btn--primary", "label": "Bright accent" },
            { "value": "btn--secondary", "label": "Secondary accent" },
            { "value": "btn--neutral", "label": "Text color" },
            { "value": "btn--black", "label": "Black" },
            { "value": "btn--white", "label": "White" }
          ]
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Article Cards",
      "blocks": [
        {
          "type": "article_card"
        },
        {
          "type": "article_card"
        },
        {
          "type": "article_card"
        },
        {
          "type": "article_card"
        }
      ]
    }
  ]
}
{% endschema %} 